export { openDialog, closeDialog, closeAllDialogs } from './dialogService';
export { default as DialogContainer } from './DialogContainer';
export { useDialog } from '@/hooks/useDialog';
export type { DialogType } from './Dialog.type';
